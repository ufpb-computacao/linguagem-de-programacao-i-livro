#include <stdio.h>
#include <stdlib.h>

int main(void) {
  printf ("sizeof (unsigned int) = %d\n", sizeof (unsigned int));
  printf ("sizeof (signed int) = %d\n", sizeof (signed int));
  printf ("sizeof (short int) = %d\n", sizeof (short int));
  system ("pause");
  return 0;
}
